# StrategyPolicyV2 (spec-only)

This package defines a specification-only `StrategyPolicyV2` contract and tagged-union `selection_plan` models.

This PR intentionally does **not** wire V2 into orchestrator/runtime execution paths.
