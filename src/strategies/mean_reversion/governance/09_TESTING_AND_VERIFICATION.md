# Testing & Verification (Governance)

## Core invariants
1) If any clause fails → allowed == False
2) If allowed == True → intent contains side, entry, stop, target
3) R:R gate enforced
4) Stop width gate enforced
5) Regime veto enforced

## Determinism
Same inputs must produce same outputs.
