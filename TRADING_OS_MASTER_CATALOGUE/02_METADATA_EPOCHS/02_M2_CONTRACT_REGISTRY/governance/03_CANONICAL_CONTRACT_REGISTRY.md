# M2 — CANONICAL CONTRACT REGISTRY

The authoritative contract registry for M2 is:

- `TRADING_OS_MASTER_CATALOGUE/02_METADATA_EPOCHS/02_M2_CONTRACT_REGISTRY/contract_registry.json`

This JSON file is the single source of truth for contract identifiers,
ownership, paths, and verification metadata.

END
