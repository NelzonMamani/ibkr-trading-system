# M2 — VERIFICATION AND PROOF

Required verification commands and outputs:

- compileall output written to audit evidence
- pytest metadata suite output written to audit evidence
- pytest full output written to audit evidence
- verify_m2_contract_registry output JSON + summary MD written to audit evidence

Certification is granted only when all required outputs exist and verification passes.

END
