# EXPECTED ARTIFACTS

Required artifacts for M3 certification:

- Governance files defining canonical modes, mode authority boundaries, and
  must/must-not behavior.
- Machine-verifiable semantics output (JSON) from
  `verification_scripts/verify_m3_mode_semantics.py`.
- Pytest evidence for M3 mode semantics tests.
- Audit evidence bundle under
  `TRADING_OS_MASTER_CATALOGUE/AUDIT_EVIDENCE/M3_MODE_SEMANTICS_CERTIFICATION/`.

END
