# DATA CLASSES

Canonical data classes include:

- Static reference data (e.g. symbol metadata)
- Session-scoped data (e.g. intraday bars)
- Commitment-scoped data (e.g. symbol hydration)
- Persistent records (e.g. trades, audits)

Each class has explicit lifecycle rules.

END
