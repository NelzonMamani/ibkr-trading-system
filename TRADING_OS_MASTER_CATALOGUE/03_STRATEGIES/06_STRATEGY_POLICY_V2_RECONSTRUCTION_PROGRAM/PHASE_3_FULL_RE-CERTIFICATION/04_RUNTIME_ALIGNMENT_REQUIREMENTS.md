# RUNTIME ALIGNMENT REQUIREMENTS

Each StrategyPolicyV2 must:

- Declare intrabar_execution explicitly.
- Declare required_fields non-empty.
- Declare safety_model with actionable rules.
- Provide exit_model with defined hard exits.

Audit engine must detect structural absence.

END
