# P03 — CODEX INSTRUCTIONS — 02_REQUIRED_MAPPINGS
You MUST implement/ensure P03 declares and utilises:
- SF_* ALLOWED/OPTIONAL/DENIED lists
- XL_* utilisation and SF→XL mapping
- REQUIRED C_* enforced before intents
- REQUIRED K_* enforced; OPTIONAL K_* mapped per SF/mode
- SCP/MCP utilised only as inputs (no direct triggers)
- LVL/ZONE/INV required per SF/XL with invalidations defined

END
