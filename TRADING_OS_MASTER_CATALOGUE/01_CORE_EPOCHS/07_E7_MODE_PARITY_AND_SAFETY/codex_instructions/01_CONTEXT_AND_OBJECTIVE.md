# Context & Objective — E7

Objective:
Certify that **SIM, PAPER, LIVE_READ_ONLY, and LIVE** behave with strict parity and safety.

Parity means:
- Same logic paths
- Same guards
- Same invariants

Differences are permitted **only** where explicitly declared
(e.g., execution provider endpoint).

This epoch is certification-first.
Apply additive fixes only.
