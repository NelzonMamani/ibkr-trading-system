# E2 CODEX — Implementation Tasks

If gaps are found:
1. Implement missing lifecycle states
2. Enforce allowed transitions only
3. Reject invalid transitions deterministically
4. Ensure lifecycle is auditable across modes

All fixes must be minimal and additive.
END
