## Context

The Trading OS must produce durable, inspectable decision artefacts across:
scanner → strategy → risk → allocation → execution.

## Objective

Certify that all material decision paths emit artefacts with:
- schema validation
- immutable storage
- replayability