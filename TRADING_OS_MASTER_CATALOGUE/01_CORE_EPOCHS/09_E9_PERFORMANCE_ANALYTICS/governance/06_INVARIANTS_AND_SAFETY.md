## Invariants & Safety

Hard invariants:
- Analytics must not block or influence execution
- Missing data must be flagged, not inferred
- Re-computation from raw data must be possible
- Metrics must be versioned if definitions change
