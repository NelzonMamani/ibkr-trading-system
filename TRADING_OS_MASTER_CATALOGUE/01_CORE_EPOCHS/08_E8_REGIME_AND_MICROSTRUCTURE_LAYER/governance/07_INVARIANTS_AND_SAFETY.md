## Invariants & Safety

Hard invariants:
- Regime layer never emits trade intents
- Regime layer never blocks execution directly
- Missing regime data must degrade gracefully
- Regime layer is mode-agnostic
