# Context & Objective — E8

Objective:
Certify that a **regime and microstructure awareness layer** exists and is wired
correctly such that:

- It is purely observational
- It is deterministic for identical inputs
- It never emits trade intents
- It never blocks execution directly
- Strategies may consume it optionally

This epoch validates wiring and safety, not alpha quality.
