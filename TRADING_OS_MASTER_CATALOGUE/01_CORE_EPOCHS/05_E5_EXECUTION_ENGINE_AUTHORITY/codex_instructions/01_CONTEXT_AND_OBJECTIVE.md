# Context & Objective — E5

Objective:
Certify and, if required, complete the **Execution Engine Authority** so that:

- There is exactly ONE lawful path to submit orders in PAPER and LIVE modes
- Execution semantics are identical between PAPER and LIVE (provider aside)
- LIVE_READ_ONLY mode is provably non-executing
- SIM mode execution is deterministic and replayable
- All execution outcomes are traceable and auditable

This epoch is **certification-first**.
Do NOT refactor architecture.
Do NOT rename epochs or modules.
Only add or harden what is missing.
