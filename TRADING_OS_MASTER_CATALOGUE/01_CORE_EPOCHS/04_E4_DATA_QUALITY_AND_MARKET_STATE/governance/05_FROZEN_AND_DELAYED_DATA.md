# E4 — Frozen & Delayed Data Handling

The system must:
- Detect frozen snapshots (e.g. IBKR non-RTH behavior)
- Distinguish frozen vs live updates
- Permit read-only analysis on frozen data
- Forbid execution if freshness thresholds are violated
